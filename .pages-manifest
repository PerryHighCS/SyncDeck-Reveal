# Paths are relative to this file's directory.
# `include` / `exclude` apply in all environments.
# `include-dev` / `exclude-dev` apply only in local dev preview.

include dist/
include chalkboard/
include-dev test/manual-regression-lab.html
